package main

/*
import (
	"encoding/csv"
	"os"
)

func main() {
	rows := [][]string{
		{"오늘", "점심", "메뉴", "뭘까"},
		{"역시", "초콜릿", "은", "엠엔엠"},
	}

	file, _ := os.Create("./file.csv")
	writer := csv.NewWriter(file)
	for _, row := range rows {
		_ = writer.Write(row)
	}
	writer.Flush()
	file.Close()
}
*/
