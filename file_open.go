package main

/*
import (
	"os"
	"path/filepath"
)

func main() {
	dpath = "./hla"
	fname = "file.txt"
	_ = os.MkdirAll(dpath, 0777)
	fpath := filepath.Join(dpath, fname)
	file, _ := os.Create(fpath)
	file.Close()
	_ = os.Remove(fpath)
	_ = os.RemoveAll(dpath)

}
*/
