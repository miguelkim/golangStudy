package main

/*
import (
	"fmt"
	"net"
)

func main() {
	ips, _ := net.LookupIP("google.com")
	for _, ip := range ips {
		fmt.Println(ip.String())
	}

	mxs, _ := net.LookupMX("naver.com")
	for _, mx := range mxs {
		fmt.Println(mx.Host, mx.Pref)
	}

	nss, _ := net.LookupNS("naver.com")
	for _, ns := range nss {
		fmt.Println(ns.Host)
	}

	names, _ := net.LookupAddr("216.58.197.174")
	for _, na := range names {
		fmt.Println(na)
	}
}
*/
