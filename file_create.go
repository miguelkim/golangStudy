package main

/*
import (
	"bufio"
	"os"
)

func main() {
	lines := []string{
		"hola",
		"korea",
		"오늘",
		"점심은뭐지",
	}
	file, _ := os.OpenFile("./file2.txt", os.O_APPEND|os.O_CREATE|os.O_WRONLY, 0644)
	writer := bufio.NewWriter(file)
	for _, line := range lines {
		_, _ = writer.WriteString(line + "\n")

	}
	writer.Flush()
	file.Close()

}
*/
