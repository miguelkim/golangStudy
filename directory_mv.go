package main

/*
import (
	"fmt"
	"os"
	"path/filepath"
)

func main() {
	scan := func(path string, i os.FileInfo, _ error) error {
		fmt.Println(i.IsDir(), path)
		return nil
	}

	_ = filepath.Walk("c:/Intel", scan)
}
*/
