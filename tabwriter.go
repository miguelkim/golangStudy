package main

/*
import (
	"fmt"
	"os"
	"text/tabwriter"
)

func main() {

	w := tabwriter.NewWriter(os.Stdin, 8, 8, 0, '\t', 0)
	fmt.Fprint(w, "\nholalalalalalalal")
	fmt.Fprint(w, "\nhalelelalsldlalsdl")

	for i := 0; i < 5; i++ {
		fmt.Fprintln(w, "this is::::", i)
	}
	w.Flush()
}
*/
