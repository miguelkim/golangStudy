package main

/*
import (
	"fmt"
	"regexp"
)

func main() {
	data := `#  123.domain.tld #`
	pattern := regexp.MustCompile(`(\d+).(\w+).\w+`)
	result := pattern.FindString(data)
	fmt.Println(result)

	groups := pattern.FindAllStringSubmatch(data, -1)
	fmt.Printf("\n%q\n", groups)
	fmt.Println(groups[0][0], groups[0][1])
}
*/
