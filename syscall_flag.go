package main

/*
import (
	"flag"
	"fmt"
)

func main() {

	name := flag.String("n", "black", "specify name. default is blank.")
	port := flag.Int("p", 8000, "specify port. default is 8000.")
	enable := flag.Bool("e", false, "specify enable. default is false.")
	flag.Parse()
	fmt.Println(*name, *port, *enable)

}
*/
